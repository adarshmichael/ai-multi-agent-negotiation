# Environment variables for NegoSim backend
# Copy this file to .env and fill in your values. Never commit .env.

# ---- Required ----
GEMINI_API_KEY=YOUR_API_KEY_HERE        # Your Google Gemini API key (get at https://aistudio.google.com)

# ---- Optional ----
PORT=8001                # Port for the backend server (must match frontend api.js BASE_URL)
MAX_ROUNDS=10            # Default maximum negotiation rounds
THINK_DELAY_MS=2000      # Milliseconds between agent messages (makes it feel live)
NODE_ENV=development     # development | production

# ---- Authentication (MongoDB + JWT) ----
MONGODB_URI=mongodb://localhost:27017/negosim   # MongoDB connection string (local or Atlas)
JWT_SECRET=your-super-secret-jwt-key-change-me  # Secret key for JWT signing (CHANGE THIS!)
JWT_EXPIRY=7d                                    # JWT token expiry duration

# ---- Google OAuth (optional) ----
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com  # Get from Google Cloud Console
